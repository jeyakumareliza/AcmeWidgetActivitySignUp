import { ActivityTypes } from './activity-types';

export class InterestedPerson {
    firstName: string;
    lastName: string;
    email: string;
    activityTypeId: number;
    comments: string;
};
